apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.streaming.name }}
  labels:
    app: {{ .Values.services.streaming.name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.services.streaming.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.streaming.name }}
    spec:
      containers:
      - name: streaming
        image: "{{ .Values.global.ecrRegistry }}/{{ .Values.services.streaming.image }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.services.streaming.port }}
        env:
        - name: PORT
          value: "{{ .Values.services.streaming.port }}"
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: mongo-uri
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: jwt-secret
        - name: CLIENT_URLS
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: client-urls
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: aws-secrets
              key: access-key-id
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: aws-secrets
              key: secret-access-key
        - name: AWS_REGION
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: aws-region
        - name: AWS_S3_BUCKET
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: s3-bucket
        - name: STREAMING_PUBLIC_URL
          value: "http://{{ .Values.services.streaming.name }}:{{ .Values.services.streaming.port }}"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /
            port: {{ .Values.services.streaming.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: {{ .Values.services.streaming.port }}
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.streaming.name }}
  labels:
    app: {{ .Values.services.streaming.name }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.services.streaming.port }}
    targetPort: {{ .Values.services.streaming.port }}
    protocol: TCP
  selector:
    app: {{ .Values.services.streaming.name }}